{
  "license": "ISC",  
  "scripts": {
    "lint:fix": "prettier */*.js \"*/**/*{.js,.ts}\" -w",
    "lint": "prettier */*.js \"*/**/*{.js,.ts}\" --check",
    "validate": "node scripts/validate_contract.js",
    "deploy": "bash scripts/deploy.sh",
    "build": "anchor build",
    "build:production": "anchor build --verifiable",
    "deploy:local": "bash scripts/deploy.sh --local",
    "deploy:devnet": "bash scripts/deploy.sh --devnet",
    "deploy:mainnet": "bash scripts/deploy.sh --mainnet",
    "test": "cd tests && ts-mocha -p ./tsconfig.json -t 1000000 ./**/*.ts",
    "test-deployed": "ts-node scripts/test_deployed_contract.ts",
    "check-wallet": "bash scripts/check_wallet.sh",
    "start-local-validator": "solana-test-validator --reset"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.0",
    "@solana/web3.js": "^1.90.1"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "prettier": "^2.6.2",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
